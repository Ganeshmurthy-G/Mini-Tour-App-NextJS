name: Deploy

on:
  workflow_run:
    workflows: ["Build"]
    types:
      - completed

jobs:
  deploy:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}

    runs-on: ubuntu-latest

    steps:
      - name: Deployment Started
        run: echo "Deploying Tourist App..."

      - name: Deployment Completed
        run: echo "Deployment Success!"
